/**
 * Self-checking multiple-choice quiz. Lives in the shared lesson kit; re-exported
 * here so this lesson's imports stay local. This lesson keeps its own `QuizQ`
 * type in `data.ts` (structurally identical).
 */
export * from "@/components/lesson/quiz";
