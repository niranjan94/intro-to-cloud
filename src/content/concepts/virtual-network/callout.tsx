/**
 * Left-barred myth/fix/note callout. Promoted to the shared lesson kit so
 * projects can reuse it; re-exported here so this lesson's imports stay put.
 */
export * from "@/components/lesson/callout";
