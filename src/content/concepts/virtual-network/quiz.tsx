/**
 * Self-checking multiple-choice quiz. Promoted to the shared lesson kit so
 * projects can reuse it; re-exported here so this lesson's imports stay put.
 * This lesson keeps its own `QuizQ` type in `data.ts` (structurally identical).
 */
export * from "@/components/lesson/quiz";
